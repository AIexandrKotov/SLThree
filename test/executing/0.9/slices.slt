using linq;

ASSERT(self, (linq.sum((1..=10)[1..2]) == 2) as is);
ASSERT(self, (linq.sum((1..=10)[1..=2]) == 5) as is);
ASSERT(self, (linq.sum((1..=10)[8..]) == 9 + 10) as is);
ASSERT(self, (linq.sum((1..=10)[..]) == linq.sum(1..=10)) as is);