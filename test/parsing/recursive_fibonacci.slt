global.fib = recursive (n) => {
    if (n < 2) { 
        return n;
    }
    else { 
        return global.fib(n - 2) + global.fib(n - 1);
    }
};
using linq;
linq.jts(linq.select(linq.range(1, 10), x => global.fib(x)));